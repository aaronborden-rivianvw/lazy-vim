return {
  {
    "akinsho/bufferline.nvim",
    opts = {
      options = {
        -- This is the key setting to display buffer numbers (buffer ID)
        numbers = "buffer_id",
      },
    },
  },
}
